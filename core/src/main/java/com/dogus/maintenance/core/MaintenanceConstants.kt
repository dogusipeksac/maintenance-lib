package com.dogus.maintenance.core

/**
 * Constants used across the maintenance library modules.
 */
object MaintenanceConstants {
    /**
     * Intent extra key for passing MaintenanceConfig between activities.
     */
    const val EXTRA_CONFIG = "extra_maintenance_config"
    
    /**
     * Default animation duration in milliseconds.
     */
    const val DEFAULT_ANIMATION_DURATION = 300L
    
    /**
     * Default fade animation duration in milliseconds.
     */
    const val DEFAULT_FADE_DURATION = 200L
    
    /**
     * Tag for logging.
     */
    const val LOG_TAG = "MaintenanceMode"
}
